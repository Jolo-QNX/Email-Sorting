Email Sorting Dashboard

The application compares both files and generates separated results for:

- Matched records
- Non-matched records
- All processed records

The dashboard also provides search, sorting, bulk Employee ID lookup, and CSV export for easier review.
